---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import PageHero from "../components/PageHero.astro";
import Footer from "../components/Footer.astro";

// 24 Adet Parke Ürünü (Sayfa başına 12 adet, toplam 2 sayfa)
const products = [
	{
		id: 1,
		name: "Vintage Rustic Çam - Seri 310",
		category: "Lamine Parke",
		color: "Açık Çam",
		price: "420,00 ₺",
		unit: "/ m²",
		desc: "Doğal budaklı rustik yapısı ve eskitme görünümü ile samimi ve sıcak yaşam alanları oluşturur.",
		img: "/images/about-2.jpg",
		link: "/parke",
	},
	{
		id: 2,
		name: "Alp Çamı Doğal Mat",
		category: "Lamine Parke",
		color: "Açık Çam",
		price: "430,00 ₺",
		unit: "/ m²",
		desc: "Yüksek dayanımlı yüzeyi ve ipeksi mat dokusuyla aydınlık, ferah ve huzurlu odalar sunar.",
		img: "/images/collection-1.jpg",
		link: "/parke",
	},
	{
		id: 3,
		name: "Loft Grafit Gri Zemin",
		category: "Vinil & Lamine",
		color: "Gri/Kül",
		price: "440,00 ₺",
		unit: "/ m²",
		desc: "Endüstriyel ve modern iç mekan tasarımları için özel olarak geliştirilmiş mat gri tonlu yüzey.",
		img: "/images/about-3.jpg",
		link: "/parke",
	},
	{
		id: 4,
		name: "Modern Sand Meşe - Seri 120",
		category: "Lamine Parke",
		color: "Açık Meşe",
		price: "450,00 ₺",
		unit: "/ m²",
		desc: "Kum beji sıcak alt tonları ve doğal ahşap damarlarıyla dingin ve modern bir atmosfer sağlar.",
		img: "/images/hero-bg.jpg",
		link: "/parke",
	},
	{
		id: 5,
		name: "Silver Grey Meşe - Seri 500",
		category: "Lamine Parke",
		color: "Gri/Kül",
		price: "460,00 ₺",
		unit: "/ m²",
		desc: "Trend gri alt tonları ve mat yüzey dokusu ile çağdaş mimari tasarımlara kusursuz uyum sağlar.",
		img: "/images/collection-1.jpg",
		link: "/parke",
	},
	{
		id: 6,
		name: "Kumsal Açık Bej Meşe",
		category: "Lamine Parke",
		color: "Açık Meşe",
		price: "460,00 ₺",
		unit: "/ m²",
		desc: "Geniş alanlarda ferahlık hissi yaratan açık renk dokusu ve çizilmez koruyucu üst katmanı.",
		img: "/images/about-1.jpg",
		link: "/parke",
	},
	{
		id: 7,
		name: "Toros Sedir Doğal Doku",
		category: "Lamine Parke",
		color: "Kızıl Kahve",
		price: "470,00 ₺",
		unit: "/ m²",
		desc: "Akdeniz sedir ağacının zengin dokusundan esinlenen, sıcak ve samimi yaşam alanları.",
		img: "/images/about-2.jpg",
		link: "/parke",
	},
	{
		id: 8,
		name: "Naturesville Meşe - Seri 110",
		category: "Masif Parke",
		color: "Açık Meşe",
		price: "480,00 ₺",
		unit: "/ m²",
		desc: "Doğal açık meşe dokusu ve yüksek yüzey dayanımı ile evinizde ferah ve aydınlık bir atmosfer oluşturur.",
		img: "/images/collection-1.jpg",
		link: "/parke",
	},
	{
		id: 9,
		name: "Kapadokya Doğal Kestane",
		category: "Lamine Parke",
		color: "Koyu Ceviz",
		price: "490,00 ₺",
		unit: "/ m²",
		desc: "Zengin hare yapısı ve derin renk geçişleriyle doğal ahşabın en sıcak ve canlı tonlarını sunar.",
		img: "/images/about-1.jpg",
		link: "/parke",
	},
	{
		id: 10,
		name: "Royal Akçaağaç Premium",
		category: "Lamine Parke",
		color: "Kızıl Kahve",
		price: "510,00 ₺",
		unit: "/ m²",
		desc: "İpeksi mat cilası ve çizilmelere ekstra dayanıklı yapısıyla yoğun kullanım alanlarına çok uygundur.",
		img: "/images/hero-bg.jpg",
		link: "/parke",
	},
	{
		id: 11,
		name: "Nordic Ash Kül Grisi - Seri 220",
		category: "Masif Parke",
		color: "Gri/Kül",
		price: "520,00 ₺",
		unit: "/ m²",
		desc: "Modern İskandinav tarzını yansıtan açık kül tonları, minimal ve modern iç mekanlar için idealdir.",
		img: "/images/about-1.jpg",
		link: "/parke",
	},
	{
		id: 12,
		name: "İskandinav Beyaz Meşe",
		category: "Lamine Parke",
		color: "Açık Meşe",
		price: "530,00 ₺",
		unit: "/ m²",
		desc: "Beyazlatılmış doğal meşe dokusuyla ferah, aydınlık ve zarif yaşam alanları oluşturur.",
		img: "/images/collection-2.jpg",
		link: "/parke",
	},
	// 2. SAYFA ÜRÜNLERİ (13 - 24)
	{
		id: 13,
		name: "Elegance Açık Kayın - Seri 200",
		category: "Masif Parke",
		color: "Açık Meşe",
		price: "540,00 ₺",
		unit: "/ m²",
		desc: "Pürüzsüz yüzey dokusu ve homojen rengiyle konforlu ve şık zemin uygulamaları sağlar.",
		img: "/images/about-3.jpg",
		link: "/parke",
	},
	{
		id: 14,
		name: "Viyana Klasik Meşe Plank",
		category: "Lamine Parke",
		color: "Doğal Ahşap",
		price: "550,00 ₺",
		unit: "/ m²",
		desc: "Avrupa klasik mimarisinden ilham alan geniş plank kesim ve derin ahşap dokusu.",
		img: "/images/collection-1.jpg",
		link: "/parke",
	},
	{
		id: 15,
		name: "Walnut Dark Koyu Ceviz - Seri 400",
		category: "Lamine Parke",
		color: "Koyu Ceviz",
		price: "560,00 ₺",
		unit: "/ m²",
		desc: "Zengin ceviz tonları ve otantik ahşap damarlarıyla yaşam alanlarınıza asil ve sıcak bir dokunuş katar.",
		img: "/images/collection-2.jpg",
		link: "/parke",
	},
	{
		id: 16,
		name: "Bosphorus Antik Meşe",
		category: "Masif Parke",
		color: "Koyu Ceviz",
		price: "580,00 ₺",
		unit: "/ m²",
		desc: "Zamanın izlerini taşıyan antik meşe dokusu ve özel el işçiliği yüzey cilası.",
		img: "/images/about-1.jpg",
		link: "/parke",
	},
	{
		id: 17,
		name: "Venedik Rustik Kestane",
		category: "Masif Parke",
		color: "Kızıl Kahve",
		price: "650,00 ₺",
		unit: "/ m²",
		desc: "Doğal budak ve hare zenginliğiyle mekanlara sıcaklık, karakter ve derinlik katar.",
		img: "/images/about-2.jpg",
		link: "/parke",
	},
	{
		id: 18,
		name: "Atlas Duman Grisi Premium",
		category: "Masif Parke",
		color: "Gri/Kül",
		price: "670,00 ₺",
		unit: "/ m²",
		desc: "Sofistike duman grisi tonları ve mat koruyucu tabakasıyla lüks iç mekanlar için tasarlandı.",
		img: "/images/hero-bg.jpg",
		link: "/parke",
	},
	{
		id: 19,
		name: "Herringbone Balıksırtı Meşe",
		category: "Lüks Seri",
		color: "Doğal Ahşap",
		price: "690,00 ₺",
		unit: "/ m²",
		desc: "Klasik balıksırtı dizilimi ve özel kilit sistemiyle salon ve prestijli alanlara şıklık kazandırır.",
		img: "/images/about-3.jpg",
		link: "/parke",
	},
	{
		id: 20,
		name: "Marmaris Doğal Tik Doku",
		category: "Masif Parke",
		color: "Kızıl Kahve",
		price: "710,00 ₺",
		unit: "/ m²",
		desc: "Yoğun neme ve suya ekstra dayanıklı yapısıyla banyo ve mutfak geçişlerinde de mükemmel.",
		img: "/images/collection-1.jpg",
		link: "/parke",
	},
	{
		id: 21,
		name: "Artisan Chevron Doğal Meşe",
		category: "Masif Parke",
		color: "Açık Meşe",
		price: "720,00 ₺",
		unit: "/ m²",
		desc: "45 derecelik açılı Fransız chevron deseniyle mekanlara derinlik ve seçkin bir estetik kazandırır.",
		img: "/images/collection-2.jpg",
		link: "/parke",
	},
	{
		id: 22,
		name: "Palermo Koyu Meşe Chevron",
		category: "Lüks Seri",
		color: "Koyu Ceviz",
		price: "740,00 ₺",
		unit: "/ m²",
		desc: "İtalyan zemin mimarisi estetiğini koyu meşe tonlarıyla buluşturan özel kesim tasarım.",
		img: "/images/about-1.jpg",
		link: "/parke",
	},
	{
		id: 23,
		name: "Monaco Balıksırtı Ceviz",
		category: "Lüks Seri",
		color: "Koyu Ceviz",
		price: "780,00 ₺",
		unit: "/ m²",
		desc: "Seçkin villalar ve prestijli ofisler için üretilmiş asil ceviz balıksırtı parke.",
		img: "/images/about-2.jpg",
		link: "/parke",
	},
	{
		id: 24,
		name: "Boğaziçi Yalı Meşesi",
		category: "Masif Parke",
		color: "Doğal Ahşap",
		price: "820,00 ₺",
		unit: "/ m²",
		desc: "Geleneksel yalı mimarisinin asaletiyle üretilen, zamansız ve ekstra kalın masif gövde.",
		img: "/images/about-3.jpg",
		link: "/parke",
	},
];

// Standart (varsayılan) sıralama: Fiyata göre artan
products.sort((a, b) => {
	const priceA = parseFloat(a.price.replace(/[^\d,]/g, "").replace(",", ".")) || 0;
	const priceB = parseFloat(b.price.replace(/[^\d,]/g, "").replace(",", ".")) || 0;
	return priceA - priceB;
});
---

<Layout title="Parke Koleksiyonlarımız - Woodcove Flooring">
	<Header />
	<PageHero
		title="Parke Koleksiyonumuz"
		subtitle="Her tarza ve ihtiyaca uygun birinci sınıf parke çeşitlerimizi keşfedin."
	/>

	<section class="catalog-section" id="catalogSection">
		<div class="container catalog-container">
			<!-- Sidebar Filters -->
			<aside class="sidebar-filters">
				<div class="sidebar-header">
					<div class="sidebar-title-wrap">
						<svg
							xmlns="http://www.w3.org/2000/svg"
							width="19"
							height="19"
							viewBox="0 0 24 24"
							fill="none"
							stroke="currentColor"
							stroke-width="2.2"
							stroke-linecap="round"
							stroke-linejoin="round"
						>
							<line x1="4" y1="21" x2="4" y2="14"></line>
							<line x1="4" y1="10" x2="4" y2="3"></line>
							<line x1="12" y1="21" x2="12" y2="12"></line>
							<line x1="12" y1="8" x2="12" y2="3"></line>
							<line x1="20" y1="21" x2="20" y2="16"></line>
							<line x1="20" y1="12" x2="20" y2="3"></line>
							<line x1="1" y1="14" x2="7" y2="14"></line>
							<line x1="9" y1="8" x2="15" y2="8"></line>
							<line x1="17" y1="16" x2="23" y2="16"></line>
						</svg>
						<h2>Filtreler</h2>
					</div>
				</div>

				<div class="filter-group">
					<h3>Kategoriler</h3>
					<ul>
						<li>
							<label>
								<input type="checkbox" class="category-filter" value="Lamine Parke" />
								<span>Lamine Parke</span>
								<span class="count-badge">(11)</span>
							</label>
						</li>
						<li>
							<label>
								<input type="checkbox" class="category-filter" value="Masif Parke" />
								<span>Masif Parke</span>
								<span class="count-badge">(9)</span>
							</label>
						</li>
						<li>
							<label>
								<input type="checkbox" class="category-filter" value="Lüks Seri" />
								<span>Lüks Seri</span>
								<span class="count-badge">(3)</span>
							</label>
						</li>
						<li>
							<label>
								<input type="checkbox" class="category-filter" value="Vinil & Lamine" />
								<span>Vinil & Lamine</span>
								<span class="count-badge">(1)</span>
							</label>
						</li>
					</ul>
				</div>

				<div class="filter-group">
					<h3>Renkler</h3>
					<div class="color-list">
						<label class="color-item">
							<input type="checkbox" class="color-filter" value="Açık Meşe" />
							<span class="color-swatch-dot" style="background-color: #D2B48C;"></span>
							<span class="color-text">Açık Meşe</span>
							<span class="count-badge">(6)</span>
						</label>
						<label class="color-item">
							<input type="checkbox" class="color-filter" value="Koyu Ceviz" />
							<span class="color-swatch-dot" style="background-color: #5C4033;"></span>
							<span class="color-text">Koyu Ceviz</span>
							<span class="count-badge">(5)</span>
						</label>
						<label class="color-item">
							<input type="checkbox" class="color-filter" value="Kızıl Kahve" />
							<span class="color-swatch-dot" style="background-color: #8B4513;"></span>
							<span class="color-text">Kızıl Kahve</span>
							<span class="count-badge">(4)</span>
						</label>
						<label class="color-item">
							<input type="checkbox" class="color-filter" value="Gri/Kül" />
							<span class="color-swatch-dot" style="background-color: #A9A9A9;"></span>
							<span class="color-text">Gri / Kül</span>
							<span class="count-badge">(4)</span>
						</label>
						<label class="color-item">
							<input type="checkbox" class="color-filter" value="Doğal Ahşap" />
							<span class="color-swatch-dot" style="background-color: #C4A482;"></span>
							<span class="color-text">Doğal Ahşap</span>
							<span class="count-badge">(3)</span>
						</label>
						<label class="color-item">
							<input type="checkbox" class="color-filter" value="Açık Çam" />
							<span class="color-swatch-dot" style="background-color: #E8D3A7;"></span>
							<span class="color-text">Açık Çam</span>
							<span class="count-badge">(2)</span>
						</label>
					</div>
				</div>

				<div class="filter-group">
					<h3>Fiyat Aralığı</h3>
					<div class="price-inputs-container">
						<div class="price-input-field">
							<label for="minPriceInput">Min (₺)</label>
							<div class="price-input-box">
								<span class="currency-sign">₺</span>
								<input
									type="number"
									id="minPriceInput"
									placeholder="400"
									min="0"
									step="10"
									aria-label="Minimum Fiyat"
								/>
							</div>
						</div>
						<span class="price-range-sep">-</span>
						<div class="price-input-field">
							<label for="maxPriceInput">Max (₺)</label>
							<div class="price-input-box">
								<span class="currency-sign">₺</span>
								<input
									type="number"
									id="maxPriceInput"
									placeholder="900"
									min="0"
									step="10"
									aria-label="Maksimum Fiyat"
								/>
							</div>
						</div>
					</div>
					<button type="button" id="applyPriceBtn" class="btn-apply-price">
						Fiyat Uygula
					</button>
				</div>

				<button type="button" class="btn-primary reset-btn" id="resetFiltersBtn">Filtreleri Temizle</button>
			</aside>

			<!-- Product List Area -->
			<div class="catalog-content">
				<!-- Top Bar: Search, Count & Sort -->
				<div class="catalog-topbar">
					<div class="catalog-search">
						<div class="search-input-wrap">
							<input
								type="text"
								id="productSearchInput"
								placeholder="Parke modellerinde ara..."
								autocomplete="off"
								aria-label="Parke modellerinde ara"
							/>
							<button id="productSearchBtn" class="search-btn" type="button" aria-label="Ara">
								<svg
									xmlns="http://www.w3.org/2000/svg"
									width="16"
									height="16"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2.5"
									stroke-linecap="round"
									stroke-linejoin="round"
								>
									<circle cx="11" cy="11" r="8"></circle>
									<line x1="21" y1="21" x2="16.65" y2="16.65"></line>
								</svg>
								<span>Ara</span>
							</button>
						</div>
					</div>

					<div class="catalog-controls">
						<div class="sort-by">
							<label for="sort">Sırala:</label>
							<select id="sort">
								<option value="price-asc" selected>Fiyata Göre Artan</option>
								<option value="price-desc">Fiyata Göre Azalan</option>
								<option value="name-asc">A'dan Z'ye</option>
								<option value="name-desc">Z'den A'ya</option>
							</select>
						</div>
						<div class="result-count">
							<strong id="productCount">{products.length}</strong> farklı ürün
						</div>
					</div>
				</div>

				<!-- Arama Sonucu Bulunamadı Mesajı -->
				<div id="noResultsMessage" class="no-results" style="display: none;">
					<p>Aradığınız filtreleme kriterine uygun ürün bulunamadı. Lütfen filtrelerinizi kontrol edin veya temizleyin.</p>
				</div>

				<!-- Product Grid -->
				<div class="product-grid" id="productGrid">
					{
						products.map((product, idx) => {
							const numericPrice = parseFloat(product.price.replace(/[^\d,]/g, "").replace(",", ".")) || 0;
							const isInitiallyVisible = idx < 12;
							return (
								<div 
									class="product-card" 
									data-name={product.name} 
									data-desc={product.desc}
									data-category={product.category}
									data-color={product.color}
									data-price={numericPrice}
									style={isInitiallyVisible ? "display: flex;" : "display: none;"}
								>
									<div class="product-img-wrapper">
										<img
											src={product.img}
											alt={product.name}
											loading="lazy"
										/>
									</div>
									<div class="product-info">
										<h3>{product.name}</h3>
										<p
											class="product-desc"
											title={product.desc}
										>
											{product.desc}
										</p>
										<div class="product-footer">
											<div class="price-box">
												<span class="price-val">
													{product.price}
												</span>
												<span class="price-unit">
													{product.unit}
												</span>
											</div>
											<a
												href={product.link}
												class="btn-review"
											>
												<span>İncele</span>
												<svg
													xmlns="http://www.w3.org/2000/svg"
													width="16"
													height="16"
													viewBox="0 0 24 24"
													fill="none"
													stroke="currentColor"
													stroke-width="2.5"
													stroke-linecap="round"
													stroke-linejoin="round"
												>
													<line
														x1="5"
														y1="12"
														x2="19"
														y2="12"
													/>
													<polyline points="12 5 19 12 12 19" />
												</svg>
											</a>
										</div>
									</div>
								</div>
							);
						})
					}
				</div>

				<!-- Pagination (2 Sayfa, Sayfa Başına 12 Ürün) -->
				<div class="pagination" id="catalogPagination">
					<button class="page-btn prev-btn" id="prevPageBtn" disabled aria-label="Önceki Sayfa">&larr;</button>
					<button class="page-btn page-num-btn active" data-page="1">1</button>
					<button class="page-btn page-num-btn" data-page="2">2</button>
					<button class="page-btn next-btn" id="nextPageBtn" aria-label="Sonraki Sayfa">&rarr;</button>
				</div>
			</div>
		</div>
	</section>

	<Footer />
</Layout>

<script>
	const sortSelect = document.getElementById("sort") as HTMLSelectElement | null;
	const productGrid = document.getElementById("productGrid");
	const searchInput = document.getElementById("productSearchInput") as HTMLInputElement | null;
	const searchBtn = document.getElementById("productSearchBtn");
	const countDisplay = document.getElementById("productCount");
	const noResultsMsg = document.getElementById("noResultsMessage");
	const paginationContainer = document.getElementById("catalogPagination");
	const prevBtn = document.getElementById("prevPageBtn") as HTMLButtonElement | null;
	const nextBtn = document.getElementById("nextPageBtn") as HTMLButtonElement | null;
	const pageNumBtns = document.querySelectorAll<HTMLButtonElement>(".page-num-btn");
	const catalogSection = document.getElementById("catalogSection");

	const categoryCheckboxes = document.querySelectorAll<HTMLInputElement>(".category-filter");
	const colorCheckboxes = document.querySelectorAll<HTMLInputElement>(".color-filter");
	const minPriceInput = document.getElementById("minPriceInput") as HTMLInputElement | null;
	const maxPriceInput = document.getElementById("maxPriceInput") as HTMLInputElement | null;
	const applyPriceBtn = document.getElementById("applyPriceBtn") as HTMLButtonElement | null;
	const resetFiltersBtn = document.getElementById("resetFiltersBtn") as HTMLButtonElement | null;

	const ITEMS_PER_PAGE = 12;
	let currentPage = 1;
	let appliedMinPrice: number | null = null;
	let appliedMaxPrice: number | null = null;

	function getFilteredAndSortedCards(): HTMLElement[] {
		if (!productGrid) return [];
		const query = (searchInput?.value || "").trim().toLocaleLowerCase("tr");
		const sortValue = sortSelect?.value || "price-asc";

		const selectedCategories = Array.from(categoryCheckboxes)
			.filter((cb) => cb.checked)
			.map((cb) => cb.value.toLocaleLowerCase("tr"));

		const selectedColors = Array.from(colorCheckboxes)
			.filter((cb) => cb.checked)
			.map((cb) => cb.value.toLocaleLowerCase("tr"));

		const cards = Array.from(productGrid.querySelectorAll<HTMLElement>(".product-card"));

		// Filter
		const matchingCards = cards.filter((card) => {
			const name = (card.getAttribute("data-name") || "").toLocaleLowerCase("tr");
			const desc = (card.getAttribute("data-desc") || "").toLocaleLowerCase("tr");
			const category = (card.getAttribute("data-category") || "").toLocaleLowerCase("tr");
			const color = (card.getAttribute("data-color") || "").toLocaleLowerCase("tr");
			const price = parseFloat(card.getAttribute("data-price") || "0");

			// Search text filter
			if (query && !name.includes(query) && !desc.includes(query) && !category.includes(query) && !color.includes(query)) {
				return false;
			}

			// Category filter
			if (selectedCategories.length > 0 && !selectedCategories.includes(category)) {
				return false;
			}

			// Color filter
			if (selectedColors.length > 0 && !selectedColors.includes(color)) {
				return false;
			}

			// Min price filter
			if (appliedMinPrice !== null && price < appliedMinPrice) {
				return false;
			}

			// Max price filter
			if (appliedMaxPrice !== null && price > appliedMaxPrice) {
				return false;
			}

			return true;
		});

		// Sort
		matchingCards.sort((a, b) => {
			const nameA = a.getAttribute("data-name") || "";
			const nameB = b.getAttribute("data-name") || "";
			const priceA = parseFloat(a.getAttribute("data-price") || "0");
			const priceB = parseFloat(b.getAttribute("data-price") || "0");

			switch (sortValue) {
				case "price-asc":
					return priceA - priceB;
				case "price-desc":
					return priceB - priceA;
				case "name-asc":
					return nameA.localeCompare(nameB, "tr");
				case "name-desc":
					return nameB.localeCompare(nameA, "tr");
				default:
					return 0;
			}
		});

		// Reorder in DOM
		matchingCards.forEach((card) => productGrid.appendChild(card));

		return matchingCards;
	}

	function renderCurrentPage(scrollToTop = false) {
		if (!productGrid) return;
		const matchingCards = getFilteredAndSortedCards();
		const allCards = Array.from(productGrid.querySelectorAll<HTMLElement>(".product-card"));
		const totalMatching = matchingCards.length;
		const totalPages = Math.max(1, Math.ceil(totalMatching / ITEMS_PER_PAGE));

		if (currentPage > totalPages) {
			currentPage = 1;
		}

		const startIndex = (currentPage - 1) * ITEMS_PER_PAGE;
		const endIndex = startIndex + ITEMS_PER_PAGE;

		// Hide all cards first
		allCards.forEach((card) => {
			card.style.display = "none";
		});

		// Show only current page matching cards
		matchingCards.slice(startIndex, endIndex).forEach((card) => {
			card.style.display = "flex";
		});

		// Update count display
		if (countDisplay) {
			countDisplay.textContent = totalMatching.toString();
		}

		// Update no results message
		if (noResultsMsg) {
			noResultsMsg.style.display = totalMatching === 0 ? "block" : "none";
		}

		// Update pagination UI
		if (paginationContainer) {
			if (totalMatching <= ITEMS_PER_PAGE) {
				pageNumBtns.forEach((btn) => {
					const page = parseInt(btn.getAttribute("data-page") || "1", 10);
					btn.style.display = page === 1 ? "flex" : "none";
					if (page === 1) btn.classList.add("active");
				});
				if (prevBtn) prevBtn.disabled = true;
				if (nextBtn) nextBtn.disabled = true;
			} else {
				pageNumBtns.forEach((btn) => {
					btn.style.display = "flex";
					const page = parseInt(btn.getAttribute("data-page") || "1", 10);
					if (page === currentPage) {
						btn.classList.add("active");
					} else {
						btn.classList.remove("active");
					}
				});
				if (prevBtn) prevBtn.disabled = currentPage === 1;
				if (nextBtn) nextBtn.disabled = currentPage === totalPages;
			}
		}

		if (scrollToTop && catalogSection) {
			const rect = catalogSection.getBoundingClientRect();
			if (rect.top < 0) {
				catalogSection.scrollIntoView({ behavior: "smooth" });
			}
		}
	}

	// Pagination button events
	pageNumBtns.forEach((btn) => {
		btn.addEventListener("click", () => {
			const page = parseInt(btn.getAttribute("data-page") || "1", 10);
			if (currentPage !== page) {
				currentPage = page;
				renderCurrentPage(true);
			}
		});
	});

	prevBtn?.addEventListener("click", () => {
		if (currentPage > 1) {
			currentPage--;
			renderCurrentPage(true);
		}
	});

	nextBtn?.addEventListener("click", () => {
		const totalMatching = getFilteredAndSortedCards().length;
		const totalPages = Math.max(1, Math.ceil(totalMatching / ITEMS_PER_PAGE));
		if (currentPage < totalPages) {
			currentPage++;
			renderCurrentPage(true);
		}
	});

	// Price Apply Button & Enter Key
	function applyPriceFilter() {
		const minVal = minPriceInput?.value.trim();
		const maxVal = maxPriceInput?.value.trim();
		appliedMinPrice = minVal ? parseFloat(minVal) : null;
		appliedMaxPrice = maxVal ? parseFloat(maxVal) : null;
		currentPage = 1;
		renderCurrentPage(false);
	}

	applyPriceBtn?.addEventListener("click", applyPriceFilter);

	[minPriceInput, maxPriceInput].forEach((input) => {
		input?.addEventListener("keydown", (e) => {
			if (e.key === "Enter") {
				e.preventDefault();
				applyPriceFilter();
			}
		});
	});

	// Category and Color Checkboxes
	categoryCheckboxes.forEach((cb) => {
		cb.addEventListener("change", () => {
			currentPage = 1;
			renderCurrentPage(false);
		});
	});

	colorCheckboxes.forEach((cb) => {
		cb.addEventListener("change", () => {
			currentPage = 1;
			renderCurrentPage(false);
		});
	});

	// Reset / Clear Filters
	function resetFilters() {
		if (searchInput) searchInput.value = "";
		if (minPriceInput) minPriceInput.value = "";
		if (maxPriceInput) maxPriceInput.value = "";
		appliedMinPrice = null;
		appliedMaxPrice = null;
		categoryCheckboxes.forEach((cb) => (cb.checked = false));
		colorCheckboxes.forEach((cb) => (cb.checked = false));
		if (sortSelect) sortSelect.value = "price-asc";
		currentPage = 1;
		renderCurrentPage(false);
	}

	resetFiltersBtn?.addEventListener("click", resetFilters);

	// Filter & Sort events
	searchBtn?.addEventListener("click", () => {
		currentPage = 1;
		renderCurrentPage(false);
	});

	searchInput?.addEventListener("input", () => {
		currentPage = 1;
		renderCurrentPage(false);
	});

	searchInput?.addEventListener("keydown", (e) => {
		if (e.key === "Enter") {
			e.preventDefault();
			currentPage = 1;
			renderCurrentPage(false);
		}
	});

	sortSelect?.addEventListener("change", () => {
		currentPage = 1;
		renderCurrentPage(false);
	});
</script>

<style>
	.catalog-section {
		padding: 80px 0;
		background: #fcfcfc;
	}
	.catalog-container {
		display: flex;
		flex-direction: column;
		gap: 40px;
	}
	@media (min-width: 1024px) {
		.catalog-container {
			flex-direction: row;
			align-items: flex-start;
		}
	}

	/* Sidebar Filters */
	.sidebar-filters {
		width: 100%;
		background: #fff;
		padding: 26px;
		border-radius: 10px;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.04);
		border: 1px solid #edf2f7;
	}
	@media (min-width: 1024px) {
		.sidebar-filters {
			width: 290px;
			flex-shrink: 0;
			position: sticky;
			top: 90px;
		}
	}

	/* Sidebar Header */
	.sidebar-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding-bottom: 18px;
		margin-bottom: 22px;
		border-bottom: 2px solid #f1f5f9;
	}
	.sidebar-title-wrap {
		display: flex;
		align-items: center;
		gap: 9px;
		color: var(--color-primary);
	}
	.sidebar-title-wrap svg {
		color: var(--color-secondary);
	}
	.sidebar-title-wrap h2 {
		font-size: 1.25rem;
		font-weight: 700;
		font-family: var(--font-primary);
		color: var(--color-primary);
		margin: 0;
	}

	/* Filter Groups */
	.filter-group {
		margin-bottom: 24px;
		padding-bottom: 20px;
		border-bottom: 1px solid #f1f5f9;
	}
	.filter-group:last-of-type {
		border-bottom: none;
		margin-bottom: 20px;
		padding-bottom: 0;
	}
	.filter-group h3 {
		font-size: 0.98rem;
		font-weight: 700;
		margin-bottom: 14px;
		color: var(--color-primary);
		font-family: var(--font-primary);
		letter-spacing: 0.2px;
	}
	.filter-group ul {
		display: flex;
		flex-direction: column;
		gap: 10px;
	}
	.filter-group label {
		display: flex;
		align-items: center;
		gap: 10px;
		font-size: 0.92rem;
		color: #475569;
		cursor: pointer;
		transition: color 0.15s ease;
	}
	.filter-group label:hover {
		color: var(--color-primary);
	}
	.filter-group input[type="checkbox"] {
		width: 17px;
		height: 17px;
		accent-color: var(--color-primary);
		cursor: pointer;
		border-radius: 4px;
	}
	.count-badge {
		margin-left: auto;
		color: #94a3b8;
		font-size: 0.82rem;
		font-weight: 500;
	}

	/* Written Color Options */
	.color-list {
		display: flex;
		flex-direction: column;
		gap: 10px;
	}
	.color-item {
		display: flex;
		align-items: center;
		gap: 10px;
		cursor: pointer;
		padding: 4px 6px;
		border-radius: 6px;
		transition: background-color 0.15s ease;
	}
	.color-item:hover {
		background-color: #f8fafc;
	}
	.color-swatch-dot {
		width: 16px;
		height: 16px;
		border-radius: 50%;
		flex-shrink: 0;
		box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.15), inset 0 1px 2px rgba(0, 0, 0, 0.1);
	}
	.color-text {
		font-size: 0.92rem;
		color: #475569;
		font-weight: 500;
	}

	/* Min-Max Price Inputs */
	.price-inputs-container {
		display: flex;
		align-items: center;
		gap: 8px;
		margin-bottom: 12px;
	}
	.price-input-field {
		flex: 1;
		display: flex;
		flex-direction: column;
		gap: 4px;
	}
	.price-input-field label {
		font-size: 0.78rem;
		font-weight: 600;
		color: #64748b;
	}
	.price-input-box {
		position: relative;
		display: flex;
		align-items: center;
	}
	.currency-sign {
		position: absolute;
		left: 10px;
		font-size: 0.85rem;
		font-weight: 600;
		color: #94a3b8;
		pointer-events: none;
	}
	.price-input-box input {
		width: 100%;
		padding: 8px 8px 8px 24px;
		border: 1px solid #cbd5e1;
		border-radius: 6px;
		font-family: var(--font-secondary);
		font-size: 0.9rem;
		outline: none;
		background: #f8fafc;
		transition: all 0.2s ease;
		-moz-appearance: textfield;
	}
	.price-input-box input::-webkit-outer-spin-button,
	.price-input-box input::-webkit-inner-spin-button {
		-webkit-appearance: none;
		margin: 0;
	}
	.price-input-box input:focus {
		background: #ffffff;
		border-color: var(--color-primary);
		box-shadow: 0 0 0 3px rgba(18, 52, 71, 0.08);
	}
	.price-range-sep {
		color: #94a3b8;
		font-weight: 600;
		margin-top: 18px;
	}
	.btn-apply-price {
		width: 100%;
		background: var(--color-primary);
		color: #ffffff;
		border: none;
		border-radius: 6px;
		padding: 9px 14px;
		font-size: 0.88rem;
		font-weight: 600;
		font-family: var(--font-secondary);
		cursor: pointer;
		transition: background-color 0.2s ease, transform 0.1s ease;
		box-shadow: 0 2px 6px rgba(18, 52, 71, 0.12);
	}
	.btn-apply-price:hover {
		background: var(--color-dark);
		transform: translateY(-1px);
	}
	.btn-apply-price:active {
		transform: translateY(0);
	}

	/* Reset Filter Button */
	.reset-btn {
		width: 100%;
		background: #f1f5f9;
		color: #475569;
		border: 1px solid #e2e8f0;
		padding: 10px;
		font-size: 0.9rem;
		font-weight: 600;
		border-radius: 6px;
		cursor: pointer;
		transition: all 0.2s ease;
		margin-top: 6px;
	}
	.reset-btn:hover {
		background: #e2e8f0;
		color: var(--color-primary);
	}

	/* Catalog Content */
	.catalog-content {
		flex: 1;
		width: 100%;
	}
	.catalog-topbar {
		display: flex;
		flex-wrap: wrap;
		justify-content: space-between;
		align-items: center;
		background: #fff;
		padding: 16px 22px;
		border-radius: 8px;
		margin-bottom: 30px;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.03);
		gap: 16px;
	}
	.catalog-search {
		flex: 1;
		min-width: 250px;
		max-width: 420px;
	}
	.search-input-wrap {
		display: flex;
		align-items: center;
		position: relative;
		width: 100%;
	}
	.search-input-wrap input {
		width: 100%;
		padding: 10px 85px 10px 14px;
		border: 1px solid #e2e8f0;
		border-radius: 6px;
		font-family: var(--font-secondary);
		font-size: 0.92rem;
		outline: none;
		transition: border-color 0.2s ease, box-shadow 0.2s ease;
		background-color: #f8fafc;
	}
	.search-input-wrap input:focus {
		background-color: #ffffff;
		border-color: var(--color-primary);
		box-shadow: 0 0 0 3px rgba(18, 52, 71, 0.08);
	}
	.search-btn {
		position: absolute;
		right: 4px;
		top: 4px;
		bottom: 4px;
		background: var(--color-primary);
		color: #ffffff;
		border: none;
		border-radius: 4px;
		padding: 0 14px;
		display: inline-flex;
		align-items: center;
		gap: 6px;
		font-weight: 600;
		font-size: 0.85rem;
		cursor: pointer;
		transition: background-color 0.2s ease;
	}
	.search-btn:hover {
		background: var(--color-dark);
	}
	.catalog-controls {
		display: flex;
		align-items: center;
		gap: 20px;
		flex-wrap: wrap;
	}
	.result-count {
		color: #666;
		font-size: 0.95rem;
	}
	.result-count strong {
		color: var(--color-primary);
	}
	.sort-by {
		display: flex;
		align-items: center;
		gap: 10px;
		font-size: 0.95rem;
		color: #555;
	}
	.sort-by select {
		padding: 8px 15px;
		border: 1px solid #ddd;
		border-radius: 4px;
		outline: none;
		font-family: var(--font-secondary);
		background-color: #fff;
		cursor: pointer;
	}
	.no-results {
		text-align: center;
		padding: 60px 20px;
		background: #ffffff;
		border-radius: 8px;
		border: 1px dashed #cbd5e1;
		color: #64748b;
		font-size: 1.05rem;
		margin-bottom: 30px;
	}

	/* Product Grid */
	.product-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: 30px;
		margin-bottom: 40px;
	}
	@media (min-width: 640px) {
		.product-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}
	@media (min-width: 1200px) {
		.product-grid {
			grid-template-columns: repeat(3, 1fr);
		}
	}

	.product-card {
		background: #fff;
		border-radius: 12px;
		overflow: hidden;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
		border: 1px solid rgba(0, 0, 0, 0.04);
		transition:
			transform 0.3s ease,
			box-shadow 0.3s ease;
		display: flex;
		flex-direction: column;
	}
	.product-card:hover {
		transform: translateY(-6px);
		box-shadow: 0 12px 30px rgba(18, 52, 71, 0.12);
	}
	.product-img-wrapper {
		position: relative;
		height: 220px;
		overflow: hidden;
		background-color: #eaeef1;
	}
	.product-img-wrapper img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: transform 0.5s ease;
	}
	.product-card:hover .product-img-wrapper img {
		transform: scale(1.06);
	}
	.product-info {
		padding: 1.5rem;
		display: flex;
		flex-direction: column;
		flex-grow: 1;
	}
	.product-info h3 {
		font-size: 1.15rem;
		font-family: var(--font-primary);
		color: var(--color-primary);
		margin-bottom: 0.6rem;
		line-height: 1.35;
		font-weight: 700;
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}
	.product-desc {
		color: #64748b;
		font-size: 0.9rem;
		line-height: 1.45;
		margin-bottom: 1.4rem;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
		text-overflow: ellipsis;
		min-height: 2.9em;
	}
	.product-footer {
		margin-top: auto;
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding-top: 1rem;
		border-top: 1px solid #edf2f7;
		gap: 0.75rem;
	}
	.price-box {
		display: flex;
		align-items: baseline;
		gap: 3px;
	}
	.price-val {
		font-size: 1.25rem;
		font-weight: 800;
		color: var(--color-primary);
		font-family: var(--font-primary);
	}
	.price-unit {
		font-size: 0.85rem;
		color: #718096;
		font-weight: 500;
	}
	.btn-review {
		background: transparent;
		color: var(--color-primary);
		padding: 4px 0;
		font-size: 0.95rem;
		font-weight: 700;
		text-decoration: none;
		display: inline-flex;
		align-items: center;
		gap: 6px;
		border: none;
		transition: color 0.25s ease;
	}
	.btn-review:hover {
		color: var(--color-secondary);
	}
	.btn-review svg {
		transition: transform 0.25s ease;
	}
	.btn-review:hover svg {
		transform: translateX(4px);
	}

	/* Pagination */
	.pagination {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 10px;
		margin-top: 20px;
	}
	.page-btn {
		width: 42px;
		height: 42px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: #fff;
		border: 1px solid #ddd;
		border-radius: 6px;
		cursor: pointer;
		font-weight: 600;
		font-size: 0.95rem;
		color: #555;
		transition: all 0.2s ease;
	}
	.page-btn.active,
	.page-btn:hover:not(:disabled) {
		background: var(--color-primary);
		color: #fff;
		border-color: var(--color-primary);
	}
	.page-btn:disabled {
		opacity: 0.4;
		cursor: not-allowed;
		background: #f1f5f9;
		border-color: #e2e8f0;
		color: #94a3b8;
	}
</style>
